dependencies {
	compileOnly group: "com.liferay.portal", name: "com.liferay.portal.kernel", version: "default"
	compileOnly group: "junit", name: "junit", version: "4.13.1"
	compileOnly project(":core:petra:petra-lang")
	compileOnly project(":core:petra:petra-string")
}

liferay {
	deployDir = file("${liferayHome}/osgi/test")
}

test {
	systemProperty "databaseHost", System.getProperty("databaseHost", "localhost")
	systemProperty "databaseName", System.getProperty("databaseName", "lportal")
	systemProperty "databasePassword", System.getProperty("databasePassword", "root")
	systemProperty "databaseUser", System.getProperty("databaseUser", "root")
	systemProperty "runCount", System.getProperty("runCount", "1")
	systemProperty "threadCount", System.getProperty("threadCount", "1")
}